${\bigl||x| - |y|\bigr|} = {\left||x| - |y|\right|}$\par
Let $p$ be any odd prime which does not divide $a(a^2 - 1)$. We take
$$m = {a^{2p} - 1 \over a^2 - 1} =
     \left({a^p - 1 \over a - 1}\right)
     \left({a^p + 1 \over a + 1}\right),\leqno(6.9.2)$$
so that $m$ is clearly composite. Now \dots
\bye
