$n \atop k = n! / k!(n-k)!,$
\hbox{this doesn't work. It needs a pair of braces.}\par\vskip0.5\baselineskip
${n \atop k} = n! / k!(n-k)!$, this looks okay, but it is missing ()'s.
$$n \atop k = n! / k!(n-k)!,\quad
\hbox{this doesn't work. It needs a pair of braces.}$$\par
$${n \atop k} = {n! \over k!(n-k)!} = n! / k!(n-k)!,\quad
\hbox{this looks okay, but it is missing the ()'s.}$$
\bye
